# Personal AI Employee — Environment Variables
# Copy to .env and fill in your values. NEVER commit .env to git.
#
# Usage:
#   cp .env.example .env
#   nano .env   (fill in your values)

# ── Vault ──────────────────────────────────────────────────────────────────────
VAULT_PATH=./AI_Employee_Vault

# ── Development Mode ───────────────────────────────────────────────────────────
DRY_RUN=false   # Set to true to log actions without executing them

# ── Gmail Watcher (Bronze/Silver Tier) ────────────────────────────────────────
# Download credentials.json from Google Cloud Console:
# https://console.cloud.google.com → APIs → Gmail API → Credentials
GMAIL_CREDENTIALS_PATH=watchers/credentials.json
GMAIL_TOKEN_PATH=watchers/token.json

# ── Email MCP Server / SMTP (Silver Tier) ─────────────────────────────────────
# Gmail SMTP: enable 2FA, then create an App Password at
#   Google Account → Security → App Passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password   # NOT your main Gmail password!
SMTP_FROM_NAME=AI Employee

# ── LinkedIn Watcher (Silver Tier) ────────────────────────────────────────────
LINKEDIN_EMAIL=your_linkedin_email@example.com
LINKEDIN_PASSWORD=your_linkedin_password
LINKEDIN_SESSION_PATH=.linkedin_session   # Persistent browser session

# ── WhatsApp Watcher (Silver Tier) ────────────────────────────────────────────
WHATSAPP_SESSION_PATH=.whatsapp_session   # Persistent browser session
# Run --setup once to scan the QR code

# ── Orchestrator ───────────────────────────────────────────────────────────────
CLAUDE_CMD=claude   # Override if claude is not in PATH

# ── Twitter/X Watcher (Gold Tier) ─────────────────────────────────────────────
# Run setup once: python3 watchers/twitter_watcher.py --vault AI_Employee_Vault --setup
TWITTER_SESSION_PATH=.twitter_session
TWITTER_HANDLE=your_twitter_handle   # Without the @ symbol

# ── Facebook Watcher (Gold Tier) ──────────────────────────────────────────────
# Run setup once: python3 watchers/facebook_watcher.py --vault AI_Employee_Vault --setup
FACEBOOK_SESSION_PATH=.facebook_session

# ── Instagram Watcher (Gold Tier) ─────────────────────────────────────────────
# Run setup once: python3 watchers/instagram_watcher.py --vault AI_Employee_Vault --setup
INSTAGRAM_SESSION_PATH=.instagram_session

# ── Odoo Accounting MCP Server (Gold Tier) ────────────────────────────────────
# Local Odoo Community setup (Docker recommended):
#   docker run -d -p 8069:8069 --name odoo odoo:17
#   Visit http://localhost:8069 → create database → set credentials below
# Leave blank to use mock/demo mode (works without Odoo installed).
ODOO_URL=http://localhost:8069
ODOO_DB=odoo
ODOO_USERNAME=admin
ODOO_PASSWORD=admin   # Change this after first login!

# ── Security Note ──────────────────────────────────────────────────────────────
# This file is in .gitignore — NEVER commit credentials.
# Rotate passwords monthly and after any suspected breach.
# For production, consider using a secrets manager (1Password CLI, etc.)
